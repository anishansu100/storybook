## What

<!-- One sentence: what does this PR do? -->

## Why

<!-- Why is this change needed? Link to issue/discussion if relevant. -->

## Checklist

- [ ] `pnpm check` passes (lint + typecheck)
- [ ] `pnpm format:write` run (or no formatting changes needed)
- [ ] DB schema changes include a migration (`pnpm db:generate`)
- [ ] No `.env` secrets committed
